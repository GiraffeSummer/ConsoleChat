const ready = (server) => {

}

const join = (server, user, type) => {
    //type == 'join'
}

const leave = (server, user, type) => {
    //type == 'leave'
}

const message = (server, message) => {

}

const createchannel = (server, user, newChannel) => {

}

const changechannel = (server, user, newChannel, oldChannel) => {

}

module.exports = { ready, join, leave, message, createchannel, changechannel }